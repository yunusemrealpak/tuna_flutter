name: chat_flutter
description: Flutter UI package for the self-hosted Chat SDK. BLoC state management, widgets, pages.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.1
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  chat_core:
    path: ../chat_core
  flutter_bloc: ^9.0.0
  bloc: ^9.0.0
  get_it: ^8.0.3
  cached_network_image: ^3.4.1
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: ^10.0.0
  mockito: ^5.4.5
  build_runner: ^2.4.15

flutter:
